node_modules/
dist/
.tmp-build/
.wrangler/
.wrangler-tmp/
.wrangler-dev.*.log
.smoke-openai-app.log

# R session residue
.Rhistory

# Generated by the e-Cidadania corpus ingestion job (scripts/ingest-ecidadania)
scripts/ingest-ecidadania/out.sql

# Harmonized dataset build output (scripts/build-dataset). Local artifact of Fase 1.2;
# freezing/versioning of releases is the release machinery (sessão C2), handled elsewhere.
# Root-anchored (leading slash) so it does NOT match src/dataset/ or tests/dataset/.
/dataset/
.dev.vars
.secrets/
reports/
# docs/ é versionado por padrão (ciência auditável do dataset + doc técnica do MCP).
# Só a área de trabalho DESCARTÁVEL é local/não-versionada (gestão, estratégia, renders .html,
# notas de gestão de sessão). Fica em disco (OneDrive), fora do git.
docs/_local/
marketing/
prompts/
.api-spec/

# Working image files — the served icon is base64-inlined in src/icon.ts
imagem-senado.jpg
icon.jpg
icon-512.jpg

# Debug screenshots (registry UIs, etc.)
*.png
naming.png

# Packaged Claude Code skill bundle (regenerated artifact)
.claude/skills/senado-br.zip
